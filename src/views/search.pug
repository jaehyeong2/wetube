extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(placeHolder="searchByTitle", type= "text", name = "keyword")
        input(type="submit",value="Search now")
    div
        each video in videos
            +video(video)